# Known Issues — fix/phase1-runtime

Lightweight running log so a reported bug can be checked against prior work
before re-investigating it. Update only when a *new, verified* issue is found
or an existing one changes status. Not a report — no narrative, no phase
recaps; those live in commit messages.

Status values: `Open` · `In Progress` · `Fixed` · `Tracked elsewhere` (already
documented outside this file, e.g. sitemapData.js's LOOSE_ENDS) · `Not built`
(confirmed with Dave as not-yet-implemented, not a defect).

| # | Issue | Status | Found | Fixed in | Notes |
|---|-------|--------|-------|----------|-------|
| 1 | `.matrix` mode text sizing classes (`text-[8px]` etc.) never applied — unescaped brackets parsed as an invalid CSS attribute selector | Fixed | Phase 1 | `9658bd3` | `src/index.css` |
| 2 | Header "Command" CTA and footer "Open Command Center" link were dead no-ops on 35+ of 40 routes | Fixed | Phase 2 | `d034f81` | `Nav.jsx` / `SiteFooter.jsx` now self-mount a `CommandCenter` fallback |
| 3 | Donorbox donation iframe (`/support`) blocked from the Payment Request API — `allowpaymentrequest` is obsolete; browsers require `allow="payment"` | Fixed | Phase 2 (found) / Phase 3 (fixed) | `79a56f5` | Likely broke Apple Pay / Google Pay in the embed |
| 4 | `/plans` hero image 404s via CORB from legacy `oohearth.app` WordPress media host | Fixed | Phase 2 | — (product-side) | Re-verified 2026-08-12: `Plans.jsx`'s hero image now points to `media.base44.com`, not the old WordPress host — `curl -I` returns 200 with a proper `access-control-allow-origin` header. Resolved by a Base44-side asset migration sometime after this was first logged, not by this repo's code. |
| 5 | `/support` never reaches `networkidle` in a headless crawl | Not a defect | Phase 2 (found) / Final pass (root-caused) | — | Root cause confirmed: `r.stripe.com/b` is Stripe.js's own fraud-detection telemetry beacon, deliberately held open — present on any page that loads Stripe.js. Not our code, not fixable, not something that should be changed. `networkidle` is simply the wrong wait condition for a page with Stripe.js on it |
| 6 | Globe's space-fog/atmosphere effect was dead — `map.setFog()` doesn't exist on installed maplibre-gl 5.24.0, threw and was silently swallowed | Fixed | Phase 4 | `da1c6f0` | `Globe3D.jsx`, ported to `setSky()` |
| 7 | Home page `LiveActivityFeed` fabricates activity events via `Math.random()` (report/claim/bust/donate/operative feed) | Fixed | Phase 4 (found) / Final pass (fixed) | `e61b023` | Removed the fabrication entirely. A correct, already-working real subscription effect (Location/DigitalBust/LeadClaim/FundingLead) sat right next to it — kept as-is, needed no changes |
| 8 | "Graph rendering" (Map priority) — no implementation found anywhere in the Map/globe/telemetry/HUD code | Not built | Phase 4 | — | Confirmed with Dave: not built yet, not a regression. Re-check this file before re-investigating if reported again |
| 9 | "Edit & Tag panel" (Map priority) — no component, button, or copy matching this name found for location/report records | Not built | Phase 4 | — | Confirmed with Dave: not built yet, not a regression. Re-check this file before re-investigating if reported again |
| 10 | No per-route SEO — `index.html`'s title/description/canonical/OG/Twitter tags are static, so every route (Map, Store, Careers, all 30+ public pages) shares the Home page's metadata | Tracked elsewhere | Phase 5 | — | Predates this branch — already surfaced in a prior design-system review ("6 decisions await Dave"). Real fix needs either a client-side head-management library (helps modern crawlers, cheap) or prerendering/SSR (helps all crawlers, expensive) — a founder call on approach + who authors 30+ pages of unique copy, not a quick patch |
| 11 | `apple-touch-icon` is an inline SVG data URI — iOS Safari does not support SVG for home-screen icons, silently ignores it | Open | Phase 5 | — | Needs a real PNG (180x180) rasterized from the existing icon design. No image tooling available in this environment (no ImageMagick/rsvg/sharp/canvas) — installing one just for this is disproportionate; do next time an image pipeline exists |
| 12 | `manifest.json` has only one SVG icon (`sizes: "any"`) — no 192/512 PNG or a maskable-purpose icon | Open | Phase 5 | — | Same tooling gap as #11. Android install prompts and Lighthouse PWA audits want raster PNG icons at standard sizes |
| 13 | `aria-hidden-focus` on `/about` (CommandCenter drawer) | Fixed | Phase 6 | `1b2c9e6` | Verified with axe-core against the prior baseline (e2e/a11y-baseline.json on feature/engineering-pipeline): 1 node -> 0. `color-contrast` counts on `/` and `/about` unchanged (12 and 10 — a design decision, intentionally not touched) |
| 14 | `CommandCenter`, `NavMenu`, `QuickCapture` overlays have no `role="dialog"`/`aria-modal`, and no focus-trap or return-focus-on-close | Open | Phase 6 (re-reviewed, Final pass) | — | Real gap, but adding role="dialog" without matching keyboard focus-trap behavior is a known anti-pattern (WAI-ARIA authoring practices) — can read as more broken to screen readers than the current silent-but-honest state. Needs a proper focus-trap implementation across all 3, consistently — re-assessed in the final pass and still judged too large/risky for a scope-locked stabilization commit. Recommended as the next engineering milestone |
| 15 | App shipped as one 4.37MB (1.21MB gzip) JS chunk — every page loaded regardless of route | Fixed | Phase 7 | `09f1d1a` | All 51 `@/pages/*` imports in `App.jsx` converted to `React.lazy()` + `Suspense`. Shared entry bundle now 538KB (176KB gzip); heavy per-route code (Globe3D, Zora, InHome) only loads when visited. Verified live across 7 routes: zero failed chunk loads |
| 16 | `@tanstack/react-query` is installed but used in only 1 place — 8 pages independently `useEffect` + raw `base44.entities.*.list()` fetch the same data with no caching/dedup, so revisiting a route re-fetches from scratch | Open | Phase 7 | — | Real gap, but migrating 8 files' state-management pattern to `useQuery` is a genuine refactor with real regression surface, not a "smallest safe fix" right before a stabilization release — flagging as a deliberate follow-up, not attempting now |
| 17 | Hydration issues | N/A | Phase 7 | — | Pure client-side-rendered SPA (Vite, no SSR) — there is no server-rendered markup to hydrate, so this checklist item doesn't apply to the current architecture |
| 18 | `npm audit`: 10 -> 2 vulnerabilities | Fixed | Phase 8 (found) / Phase 9 (fully closed) | `6a6e8cc`, `d3b35d7` | nanoid/postcss/dompurify/socket.io-parser patched non-breaking (`6a6e8cc`). Remaining 2 needed breaking-change decisions: quill/react-quill was already dead code, removed in Phase 2 (`7a1e54d`); react-router-dom's open-redirect CVE closed by the v7.18.2 migration (`d3b35d7`, PR #52). Re-verified 2026-08-12: `npm audit --omit=dev` -> 0 vulnerabilities |
| 19 | No Content-Security-Policy anywhere (no meta tag, no hosting header config found in-repo) | Open — external blocker confirmed | Phase 8 (found) / this session (blocker confirmed) | — | Full directive/origin scoping done, see `FINAL_ENGINEERING_AUDIT.md` §13. Re-confirmed this session: this repo has **no hosting/deploy config at all** (`grep` for `netlify.toml`/`vercel.json`/`_headers` finds nothing) — deployment happens entirely in Base44's own pipeline, outside this repo's visibility. A `Content-Security-Policy-Report-Only` header can only be set via an HTTP response header (the `<meta>` tag mechanism does not support report-only mode per spec), so shipping even a safe report-only policy is not something this repo alone can do. **External action needed from Dave**: confirm what mechanism Base44's hosting exposes for custom response headers (if any), and provide the live `appBaseUrl`/WebSocket origin + the radio-station source list so the policy in §13 can be finalized and handed off for that header to be set. Engineering has done everything possible without that access. |
| 20 | Two OSM map iframes (`LocationDetail`, `BusStopDetail`) had no `sandbox`/`referrerpolicy` | Fixed | Phase 8 | `2dd31f8` | Verified live: sandboxed embed still loads real tiles/content. Donorbox's iframe deliberately left un-sandboxed — needs form/popup permissions to process payments, too risky to restrict without a real checkout to test against |
| 21 | Upload validation — no client-side file-size/type limit on photo-upload call sites | Fixed | This session (2026-08-12) | `0cb676a` | Original count (5, named `FieldReport`/`QuickCapture`/`TrashId`/`ArLens`/`MintLocationPanel`) was stale — re-verified via `grep` at fix time: 11 real `UploadFile` call sites exist now. Of those, 9 are genuine user-photo uploads and got a shared `src/lib/validateUpload.js` (size cap, MIME allowlist, extension cross-check, magic-byte signature check). 2 deliberately excluded: `MintLocationPanel.jsx` uploads generated JSON metadata (not a photo), `ArLens.jsx` uploads a `canvas.toBlob()` frame capture (never user-selected bytes, no untrusted-input surface). **Remaining gap, unchanged by this fix:** whether Base44's own `Core.UploadFile` integration enforces anything server-side is still unknown from this repo — client-side validation is a UX/abuse-reduction gate, not a security boundary on its own. Needs checking against the Base44 platform docs/dashboard directly, still not a guess |
| 22 | No hardcoded secrets, no `console.log` debug leftovers anywhere in `src/` | Verified clean | Phase 8 | — | Grepped for key-shaped literals (`sk_live`, `AIza...`, `ghp_...`, PEM headers) — none found. All external URLs hardcoded in `base44/functions/*` are public, keyless endpoints (Solana/Polygon public RPCs, CoinGecko, Blockchair, the oohearth.app WordPress feed) |
| 23 | `investorAccess/entry.ts` fell back to a hardcoded, publicly-readable default access code + derived HMAC secret when `INVESTOR_ACCESS_CODE`/`INVESTOR_TOKEN_SECRET` weren't set in Base44 Secrets — bypassable and token-forgeable by anyone reading this public repo | Fixed | This session (2026-08-11) | `206db4b` | Now fails closed (503) when either secret is missing, matching the established pattern already used by `personaCtl`/`n8nPing`/`stripeWebhook` in the same directory |
| 24 | Command Center "Bangkok"/"London" links pointed to `oohearth.app/area/{city}` — no such route exists anywhere in `App.jsx` | Fixed | This session (2026-08-11), reported by Dave via annotated screenshot | `1e062ca` | Repointed both to `/map` — no city-filtered-map route exists to deep-link into instead (`Map.jsx` has no URL query-param filtering) |
| 25 | Command Center "@advertisersanonymous" link points to `oohearth.app/profile/advertisersanonymous` — same broken-route bug as #24, but no safe equivalent destination exists (no `/profile/:handle` route) | Fixed | This session (2026-08-11) | — (product-side) | Re-verified 2026-08-12: `CommandCenter.jsx` now links to `to: "/operative"`, and `/operative` is a real registered route (`App.jsx`) rendering `OperativeProfile`. The founder decision this row asked for ("remove the link, or build a real profile page") appears to have been made — the real page got built, likely via Base44, after this was first logged. |
| 26 | `js/xss-through-dom` — `TrashId.jsx`, `MintLocationPanel.jsx` (DOM text reinterpreted as HTML without escaping) | Ruled out — false positive | This session (2026-08-12), via GitHub native CodeQL PR check | — | Investigated via each alert's own code-flow sink location, not just the summary line: both point at plain controlled `<input onChange>` handlers (`e.target.value` / `e.target.files`). No DOM-to-HTML reinterpretation exists at either site. Left open on GitHub (not dismissed) since there's no dismiss-with-evidence workflow needed here — genuinely nothing to fix |
| 27 | `js/incomplete-multi-character-sanitization` — `fieldNews/entry.ts`, `importKmlLocations/entry.ts`, `fetchMapLocations/entry.ts` (HTML-tag-stripping regex left `<script` fragments after a single pass) | Fixed | This session (2026-08-12) | `0aa9afa` | Fixed-point tag-strip loop (repeat until a pass makes no further change) in all three. Verified with a reproduction, not just code review: `stripTags('<scr<script>ipt>alert(1)</scr</script>ipt>')` → `'ipt>alert(1)ipt>'`, no reconstituted tag. Confirmed fixed by GitHub's own rescan of `main` post-merge (`fixed_at` set on alerts #4/#22/#23), not just local testing |
| 28 | `js/double-escaping` — `fieldNews/entry.ts` (`&amp;` unescape before `&lt;`/`&gt;` let a double-encoded payload cascade to a live tag) | Fixed | This session (2026-08-12) | `0aa9afa` | Replaced chained sequential `.replace()` entity decoding with a single-pass lookup-table replacer, and moved decode-before-strip (was strip-before-decode, which let an entity-encoded tag survive stripping and only become live once decoded afterward). Verified: `decode('&amp;lt;script&amp;gt;alert(1)&amp;lt;/script&amp;gt;')` → stays `'&lt;script&gt;...'`, never reaches a literal `<`. Confirmed fixed by GitHub's rescan (alert #21) |
| 29 | Prettier's `--write` broke a JSDoc type-cast in `imageCompress.js` (stripped parens, shifted what was being cast from `file` to `file.name \|\| 'photo'`) | Fixed | This session (2026-08-12), during the Prettier backlog PR | — | Caught by typecheck immediately after the format pass (2 new TS2339 errors), not missed. Fixed and guarded with `// prettier-ignore` on that exact line so a future `npm run format` can't reintroduce it |
| 30 | `js/useless-conditional` — `TelemetryBar.jsx`'s air-quality fetch effect declared `let cancelled = false` and its cleanup set `cancelled = false` again (typo for `true`), so both `if (!cancelled)` guards were dead code and `setPm`/`setPmLoading` could fire after unmount | Fixed | Phase 7 (2026-08-12) | — | One-character fix: cleanup now sets `cancelled = true`. Found via CodeQL native code-scanning alerts, not previously surfaced |
| 31 | CodeQL `js/unused-local-variable` — 12 open notes across `Globe3D.jsx`, `Hero.jsx`, `App.jsx`, `TokenChip3D.jsx`, `ChipExplodedView.jsx`, `GraffitiCamera.jsx`, `GraffitiCam.jsx`, `Book.jsx`, `ArLens.jsx`, `fetchMapLocations/entry.ts`, `generateOgImage/entry.ts` | Open | Phase 7 (2026-08-12) | — | Note-severity only, zero functional/security impact, not caught by the repo's own ESLint config. Deferred as backlog — not worth a dedicated PR right now |
