# Known Issues — fix/phase1-runtime

Lightweight running log so a reported bug can be checked against prior work
before re-investigating it. Update only when a *new, verified* issue is found
or an existing one changes status. Not a report — no narrative, no phase
recaps; those live in commit messages.

Status values: `Open` · `In Progress` · `Fixed` · `Tracked elsewhere` (already
documented outside this file, e.g. sitemapData.js's LOOSE_ENDS) · `Not built`
(confirmed with Dave as not-yet-implemented, not a defect).

| # | Issue | Status | Found | Fixed in | Notes |
|---|-------|--------|-------|----------|-------|
| 1 | `.matrix` mode text sizing classes (`text-[8px]` etc.) never applied — unescaped brackets parsed as an invalid CSS attribute selector | Fixed | Phase 1 | `9658bd3` | `src/index.css` |
| 2 | Header "Command" CTA and footer "Open Command Center" link were dead no-ops on 35+ of 40 routes | Fixed | Phase 2 | `d034f81` | `Nav.jsx` / `SiteFooter.jsx` now self-mount a `CommandCenter` fallback |
| 3 | Donorbox donation iframe (`/support`) blocked from the Payment Request API — `allowpaymentrequest` is obsolete; browsers require `allow="payment"` | Fixed | Phase 2 (found) / Phase 3 (fixed) | `79a56f5` | Likely broke Apple Pay / Google Pay in the embed |
| 4 | `/plans` hero image 404s via CORB from legacy `oohearth.app` WordPress media host | Fixed | Phase 2 | — (product-side) | Re-verified 2026-08-12: `Plans.jsx`'s hero image now points to `media.base44.com`, not the old WordPress host — `curl -I` returns 200 with a proper `access-control-allow-origin` header. Resolved by a Base44-side asset migration sometime after this was first logged, not by this repo's code. |
| 5 | `/support` never reaches `networkidle` in a headless crawl | Not a defect | Phase 2 (found) / Final pass (root-caused) | — | Root cause confirmed: `r.stripe.com/b` is Stripe.js's own fraud-detection telemetry beacon, deliberately held open — present on any page that loads Stripe.js. Not our code, not fixable, not something that should be changed. `networkidle` is simply the wrong wait condition for a page with Stripe.js on it |
| 6 | Globe's space-fog/atmosphere effect was dead — `map.setFog()` doesn't exist on installed maplibre-gl 5.24.0, threw and was silently swallowed | Fixed for real (this session, 2026-08-29) — was NOT actually fixed on `origin/main` despite this row | Phase 4 (claimed) / this session (verified + re-fixed) | `da1c6f0` (orphaned, never merged) / this session's commit | **Real drift, not stale docs review:** `da1c6f0` ("ported to setSky()") is on a local branch (`fix/phase1-runtime`) that was never merged — confirmed via `git merge-base --is-ancestor da1c6f0 origin/main` (false). `origin/main`'s own `Globe3D.jsx` still had the dead `map.setFog()` call this whole time; only this row's text landed, not the code. **Also: that orphaned commit's own mapping would not have visually worked even if merged** — it used `fog-color`/`horizon-fog-blend`, which `@maplibre/maplibre-gl-style-spec`'s own types document as "Requires 3D terrain," and `Globe3D.jsx` sets up no terrain (`grep -n setTerrain` — zero hits). Re-fixed using the sky/horizon/atmosphere properties that apply without terrain (`sky-color`, `horizon-color`, `sky-horizon-blend`, `atmosphere-blend`). Verified live, not by inference: instrumented the real call temporarily, confirmed `map.getSky()` reads back the exact values set with zero exception, against a real Chromium+swiftshader render of `/` (`GlobeSection`) — then removed the instrumentation before committing |
| 7 | Home page `LiveActivityFeed` fabricates activity events via `Math.random()` (report/claim/bust/donate/operative feed) | Fixed | Phase 4 (found) / Final pass (fixed) | `e61b023` | Removed the fabrication entirely. A correct, already-working real subscription effect (Location/DigitalBust/LeadClaim/FundingLead) sat right next to it — kept as-is, needed no changes |
| 8 | "Graph rendering" (Map priority) — no implementation found anywhere in the Map/globe/telemetry/HUD code | Not built | Phase 4 | — | Confirmed with Dave: not built yet, not a regression. Re-check this file before re-investigating if reported again |
| 9 | "Edit & Tag panel" (Map priority) — no component, button, or copy matching this name found for location/report records | Not built | Phase 4 | — | Confirmed with Dave: not built yet, not a regression. Re-check this file before re-investigating if reported again |
| 10 | No per-route SEO — `index.html`'s title/description/canonical/OG/Twitter tags are static, so every route (Map, Store, Careers, all 30+ public pages) shares the Home page's metadata | Fixed | Phase 5 (found) / PR #102 (fixed) | `cf8df31` | Re-verified this session: `scripts/prerender-meta.mjs` now bakes per-route title/description/canonical/OG/Twitter metadata into 57 static HTML routes at build time (confirmed live via `npm run build` this session — 57 routes written, no errors). This row was stale — the fix landed after it was last updated |
| 11 | `apple-touch-icon` is an inline SVG data URI — iOS Safari does not support SVG for home-screen icons, silently ignores it | Fixed | Phase 5 (found) / this session (fixed) | — | Previously blocked on "no image tooling in this environment" — re-checked rather than trusting that note: Playwright (already a devDependency, used for e2e) can rasterize SVG via a real headless-Chromium screenshot, no new dependency needed. `scripts/generate-app-icons.mjs` rasterizes `public/brand/oohearth-mark.svg` (the manifest's existing canonical mark, not the separate lower-fidelity inline SVGs) to 180/192/512px PNGs. `index.html`'s `apple-touch-icon` now points at the real 180px PNG. Verified: correct pixel dimensions (`file` on each output), visually inspected (renders the actual globe/orbital mark, not blank/corrupt), `dist/brand/*.png` present after `npm run build` with the built `index.html`'s tag resolving to it |
| 12 | `manifest.json` has only one SVG icon (`sizes: "any"`) — no 192/512 PNG or a maskable-purpose icon | Fixed | Phase 5 (found) / this session (fixed) | — | Same fix as #11. Added explicit `192x192`/`512x512` PNG entries (`purpose: "any maskable"`, matching the existing SVG entry's declared purpose — not a new claim, the source mark's maskable-safe-zone padding is unchanged either way) alongside the original SVG entry, which stays for browsers that prefer vector. Verified: `dist/manifest.json` is valid JSON post-build, all three icon files present in `dist/brand/` |
| 13 | `aria-hidden-focus` on `/about` (CommandCenter drawer) | Fixed | Phase 6 | `1b2c9e6` | Verified with axe-core against the prior baseline (e2e/a11y-baseline.json on feature/engineering-pipeline): 1 node -> 0. `color-contrast` counts on `/` and `/about` unchanged (12 and 10 — a design decision, intentionally not touched) |
| 14 | `CommandCenter`, `NavMenu`, `QuickCapture` overlays have no `role="dialog"`/`aria-modal`, and no focus-trap or return-focus-on-close | Fixed | Phase 6 (found) / a11y convergence session (fixed) | — | Shared `src/hooks/useFocusTrap.js` hook: moves focus into the container on open, traps Tab/Shift+Tab within its real focusable elements, restores focus to the trigger on close, sets `role="dialog"`/`aria-modal`/`aria-label`. Caught and fixed a real bug during implementation, not just a test artifact: `el.offsetParent !== null` (the standard "is this visible" check) is **always null for `position:fixed` elements** regardless of actual visibility, and every overlay in this app is `fixed inset-0` — the initial version silently found zero focusable elements in every container and fell back to focusing the container itself every time, defeating Tab-trapping. Fixed by checking `getBoundingClientRect()` has non-zero size instead. Verified with real keyboard-navigation Playwright tests (`e2e/focus-trap.spec.ts`, desktop + mobile), not visual review — confirmed focus actually lands on real interactive elements, not just the container fallback. `NavMenu` needed two independent trap instances (mobile launcher + desktop popover are both always mounted, toggled via Tailwind `md:` classes, so wrapping them in one shared container collapsed to a zero-size box since `position:fixed` children don't size a static parent). The other 7 ad-hoc-`Escape`-handling overlays found in the same audit (`FieldCheckCamera`, `GraffitiCamera`, `LabSignupModal`, `UnitFinder`, `Walkthrough`, `AccountMenu`, `MapSearch`) can now adopt this same hook cheaply in follow-up PRs — not done here, kept this PR bounded to the 3 originally-flagged components |
| 15 | App shipped as one 4.37MB (1.21MB gzip) JS chunk — every page loaded regardless of route | Fixed | Phase 7 | `09f1d1a` | All 51 `@/pages/*` imports in `App.jsx` converted to `React.lazy()` + `Suspense`. Shared entry bundle now 538KB (176KB gzip); heavy per-route code (Globe3D, Zora, InHome) only loads when visited. Verified live across 7 routes: zero failed chunk loads |
| 16 | `@tanstack/react-query` is installed but used in only 2 places — pages independently `useEffect` + raw `base44.entities.*.list()`/`.filter()` fetch the same data with no caching/dedup, so revisiting a route re-fetches from scratch | Open (6/20 migrated) | Phase 7 (found) / re-scoped + first pilot / this session (2nd/3rd/4th pilots) | `StoreAdmin.jsx`, `Careers.jsx` + `CareerRole.jsx`, `LabHub.jsx` | Original count (8) was stale — re-verified via grep: 20 real call sites (`CareersAdmin`, `BusStopDetail`, `FieldId`, `Account`, `MediaCorps`, `InHome`, `LabAdmin`, `Map`, `Store`, `CareerRole`, `LabHub`, `Careers`, `FdePortal`, `LocationDetail`, `AtariPortfolio`, `Plans`, `portals/AdbustingPortal`, `Dashboard`, `StoreAdmin`, `PortalOps`, `portals/GraffitiPortal`). **`LabAdmin.jsx` — hard-stopped, not migrated.** Its loader is read → conditional-write (auto-provisions missing `LabPrototype` records) → reread, not a pure query. **`StoreAdmin.jsx` — migrated** (see prior entry). **`Careers.jsx` + `CareerRole.jsx` — migrated together, a genuinely better case than a single-page pilot:** both pages independently ran their own copy-pasted `useLiveRoles()` hook fetching the identical `CareerRoleStatus.list('sort_order')` and merging it onto static `roles.js` content — real duplicate code AND a real duplicate network request every time a visitor went list→detail. Replaced both with one shared `src/hooks/useCareerRoles.js` (`useQuery({queryKey:['career-role-status'],...})`), each page keeping its own downstream filter/derivation exactly as before. The "keep static fallback on error" behavior is preserved by construction (`if (!data?.length) return BASE_ROLES` covers both loading and error states, matching the old try/catch's silent-fallback). Verified safe against the existing e2e mock's default (`CareerRoleStatus` isn't mocked; unmocked GETs return `[]`, which is the same fallback path the old code hit too — traced, not assumed). Added `e2e/careers.spec.ts` (list + detail render from the static fallback). Verified: lint/typecheck/build clean, `npm run format:check` clean, full Playwright suite 154/155 passing single-worker plus 2 new tests (1 unrelated `file-picker-keyboard.spec.ts` failure reproduced as a flake — 3/3 passes in isolation and passes clean on unmodified `main`, confirmed unrelated before dismissing it, not assumed). **`LabHub.jsx` — migrated, but a real, larger duplicate-fetch problem was found and deliberately NOT touched:** `NavMenu.jsx` (rendered globally, on every page) and `LabAccessRoute.jsx` (the access-control guard wrapping every `/lab/*` route) each run their *own* independent `LabPrototype` reads — `NavMenu.jsx` a `.list()` plus a live `.subscribe()`, `LabAccessRoute.jsx` a per-route `.filter({path})`. This is the single highest-value remaining duplicate-fetch case in the app (one fetch could serve all three consumers instead of three), but deliberately deferred: `LabAccessRoute.jsx` gates access to lab pages — a caching layer there risks a stale-access window (e.g. access revoked but a cached query still grants entry) unless staleness is handled very carefully, and `NavMenu.jsx`'s live `.subscribe()` doesn't have a drop-in `useQuery` equivalent without deliberately wiring `queryClient.invalidateQueries()` into the subscription callback. `LabHub.jsx`'s own query key (`['lab-prototypes']`) was deliberately chosen so a future migration of the other two can share it. Added `e2e/lab-hub.spec.ts` (no prior coverage of `/lab`). Verified: lint/typecheck/format/build clean. **`LocationDetail.jsx` — migrated (this session, 2026-08-29):** read-only fallback chain (get by id -> filter by legacy source_link -> static seed data) wrapped as one queryFn, keyed `['location', id]`. `LocationEditPanel`'s `onUpdated` callback already receives the full fresh record after a moderator edit, so it now writes directly into the query cache via `queryClient.setQueryData` instead of local `setState` — avoids a redundant re-fetch after an edit too. "Not found" (chain exhausts, rec stays null) is a valid cached result, not an error — the page already has a dedicated empty state. Verified: `e2e/location-detail.spec.ts` (4 tests) passes serially and parallel; adversarially confirmed the actual caching claim with a throwaway test exercising real client-side navigation + `page.goBack()` (1 network call across 2 visits, not 2) before trusting it — a first attempt using `page.goto()` as a fallback nav path gave a false failure, since `goto()` is a full reload that wipes the QueryClient regardless of this fix; diagnosed as a test defect before concluding anything. **`FieldId.jsx` — migrated (this session, 2026-08-30):** a single flat, read-only `Operative.list()` call (roster picker for a credential-card generator) wrapped as `useQuery(['operatives'], ...)`. No mutation/cache-write path needed — nothing on this page ever updates an `Operative` record. Added `e2e/field-id.spec.ts` (no prior coverage existed): one test for the no-live-records render path (same pattern as `lab-hub.spec.ts`), one that proves the actual caching claim via real client-side navigation + `page.goBack()`, asserting the mocked `Operative.list()` network call fires exactly once across two visits (guarded against a false-positive zero-count pass). Remaining 14 files not yet inspected — do not assume they're safe until each is checked individually, `LabAdmin` proves the pattern isn't uniform |
| 17 | Hydration issues | N/A | Phase 7 | — | Pure client-side-rendered SPA (Vite, no SSR) — there is no server-rendered markup to hydrate, so this checklist item doesn't apply to the current architecture |
| 18 | `npm audit`: 10 -> 2 vulnerabilities | Fixed | Phase 8 (found) / Phase 9 (fully closed) | `6a6e8cc`, `d3b35d7` | nanoid/postcss/dompurify/socket.io-parser patched non-breaking (`6a6e8cc`). Remaining 2 needed breaking-change decisions: quill/react-quill was already dead code, removed in Phase 2 (`7a1e54d`); react-router-dom's open-redirect CVE closed by the v7.18.2 migration (`d3b35d7`, PR #52). Re-verified 2026-08-12: `npm audit --omit=dev` -> 0 vulnerabilities |
| 19 | No Content-Security-Policy anywhere (no meta tag, no hosting header config found in-repo) | Open — confirmed structural, not just unconfigured | Phase 8 (found) / 2026-08-12 (blocker suspected) / 2026-08-23 (confirmed via Base44's own docs) | — | Full directive/origin scoping done and re-verified against current `main`, see `FINAL_ENGINEERING_AUDIT.md` §13. This repo has no hosting/deploy config at all — deployment happens entirely in Base44's own pipeline. Checked `docs.base44.com` directly this session: Base44's custom-domain docs confirm DNS points straight at their infrastructure and they issue the TLS cert — **no documented mechanism exists, in this repo or via DNS, to set custom response headers on a Base44-hosted app.** A `Content-Security-Policy-Report-Only` header can only be set via an HTTP response header (the `<meta>` tag mechanism does not support report-only mode per spec). **External action needed from Dave**: either Base44 ships a native header-config feature (track via their support), or Dave decides to front the domain with a CDN/proxy he controls — a real infrastructure decision with its own risk (could interfere with Base44's automatic cert issuance), not something to do unilaterally. Engineering has done everything verifiable without that decision. |
| 20 | Two OSM map iframes (`LocationDetail`, `BusStopDetail`) had no `sandbox`/`referrerpolicy` | Fixed | Phase 8 | `2dd31f8` | Verified live: sandboxed embed still loads real tiles/content. Donorbox's iframe deliberately left un-sandboxed — needs form/popup permissions to process payments, too risky to restrict without a real checkout to test against |
| 21 | Upload validation — no client-side file-size/type limit on photo-upload call sites | Fixed | This session (2026-08-12) | `0cb676a` | Original count (5, named `FieldReport`/`QuickCapture`/`TrashId`/`ArLens`/`MintLocationPanel`) was stale — re-verified via `grep` at fix time: 11 real `UploadFile` call sites exist now. Of those, 9 are genuine user-photo uploads and got a shared `src/lib/validateUpload.js` (size cap, MIME allowlist, extension cross-check, magic-byte signature check). 2 deliberately excluded: `MintLocationPanel.jsx` uploads generated JSON metadata (not a photo), `ArLens.jsx` uploads a `canvas.toBlob()` frame capture (never user-selected bytes, no untrusted-input surface). **Remaining gap, unchanged by this fix:** whether Base44's own `Core.UploadFile` integration enforces anything server-side is still unknown from this repo — client-side validation is a UX/abuse-reduction gate, not a security boundary on its own. Needs checking against the Base44 platform docs/dashboard directly, still not a guess |
| 22 | No hardcoded secrets, no `console.log` debug leftovers anywhere in `src/` | Verified clean | Phase 8 | — | Grepped for key-shaped literals (`sk_live`, `AIza...`, `ghp_...`, PEM headers) — none found. All external URLs hardcoded in `base44/functions/*` are public, keyless endpoints (Solana/Polygon public RPCs, CoinGecko, Blockchair, the oohearth.app WordPress feed) |
| 23 | `investorAccess/entry.ts` fell back to a hardcoded, publicly-readable default access code + derived HMAC secret when `INVESTOR_ACCESS_CODE`/`INVESTOR_TOKEN_SECRET` weren't set in Base44 Secrets — bypassable and token-forgeable by anyone reading this public repo | Fixed | This session (2026-08-11) | `206db4b` | Now fails closed (503) when either secret is missing, matching the established pattern already used by `personaCtl`/`n8nPing`/`stripeWebhook` in the same directory |
| 24 | Command Center "Bangkok"/"London" links pointed to `oohearth.app/area/{city}` — no such route exists anywhere in `App.jsx` | Fixed | This session (2026-08-11), reported by Dave via annotated screenshot | `1e062ca` | Repointed both to `/map` — no city-filtered-map route exists to deep-link into instead (`Map.jsx` has no URL query-param filtering) |
| 25 | Command Center "@advertisersanonymous" link points to `oohearth.app/profile/advertisersanonymous` — same broken-route bug as #24, but no safe equivalent destination exists (no `/profile/:handle` route) | Fixed | This session (2026-08-11) | — (product-side) | Re-verified 2026-08-12: `CommandCenter.jsx` now links to `to: "/operative"`, and `/operative` is a real registered route (`App.jsx`) rendering `OperativeProfile`. The founder decision this row asked for ("remove the link, or build a real profile page") appears to have been made — the real page got built, likely via Base44, after this was first logged. |
| 26 | `js/xss-through-dom` — `TrashId.jsx`, `MintLocationPanel.jsx` (DOM text reinterpreted as HTML without escaping) | Ruled out — false positive | This session (2026-08-12), via GitHub native CodeQL PR check | — | Investigated via each alert's own code-flow sink location, not just the summary line: both point at plain controlled `<input onChange>` handlers (`e.target.value` / `e.target.files`). No DOM-to-HTML reinterpretation exists at either site. Left open on GitHub (not dismissed) since there's no dismiss-with-evidence workflow needed here — genuinely nothing to fix |
| 27 | `js/incomplete-multi-character-sanitization` — `fieldNews/entry.ts`, `importKmlLocations/entry.ts`, `fetchMapLocations/entry.ts` (HTML-tag-stripping regex left `<script` fragments after a single pass) | Fixed | This session (2026-08-12) | `0aa9afa` | Fixed-point tag-strip loop (repeat until a pass makes no further change) in all three. Verified with a reproduction, not just code review: `stripTags('<scr<script>ipt>alert(1)</scr</script>ipt>')` → `'ipt>alert(1)ipt>'`, no reconstituted tag. Confirmed fixed by GitHub's own rescan of `main` post-merge (`fixed_at` set on alerts #4/#22/#23), not just local testing |
| 28 | `js/double-escaping` — `fieldNews/entry.ts` (`&amp;` unescape before `&lt;`/`&gt;` let a double-encoded payload cascade to a live tag) | Fixed | This session (2026-08-12) | `0aa9afa` | Replaced chained sequential `.replace()` entity decoding with a single-pass lookup-table replacer, and moved decode-before-strip (was strip-before-decode, which let an entity-encoded tag survive stripping and only become live once decoded afterward). Verified: `decode('&amp;lt;script&amp;gt;alert(1)&amp;lt;/script&amp;gt;')` → stays `'&lt;script&gt;...'`, never reaches a literal `<`. Confirmed fixed by GitHub's rescan (alert #21) |
| 29 | Prettier's `--write` broke a JSDoc type-cast in `imageCompress.js` (stripped parens, shifted what was being cast from `file` to `file.name \|\| 'photo'`) | Fixed | This session (2026-08-12), during the Prettier backlog PR | — | Caught by typecheck immediately after the format pass (2 new TS2339 errors), not missed. Fixed and guarded with `// prettier-ignore` on that exact line so a future `npm run format` can't reintroduce it |
| 30 | `js/useless-conditional` — `TelemetryBar.jsx`'s air-quality fetch effect declared `let cancelled = false` and its cleanup set `cancelled = false` again (typo for `true`), so both `if (!cancelled)` guards were dead code and `setPm`/`setPmLoading` could fire after unmount | Fixed | Phase 7 (2026-08-12) | — | One-character fix: cleanup now sets `cancelled = true`. Found via CodeQL native code-scanning alerts, not previously surfaced |
| 31 | CodeQL `js/unused-local-variable` — 12 open notes across `Globe3D.jsx`, `Hero.jsx`, `App.jsx`, `TokenChip3D.jsx`, `ChipExplodedView.jsx`, `GraffitiCamera.jsx`, `GraffitiCam.jsx`, `Book.jsx`, `ArLens.jsx`, `fetchMapLocations/entry.ts`, `generateOgImage/entry.ts` | Fixed | Phase 7 (found) / this session (fixed) | — | Note-severity only, zero functional/security impact, not caught by the repo's own ESLint config. Each of the 12 individually checked for real usage (not blind deletion) before removal — 2 were "dead getter, live setter" `useState` cases fixed via array-hole destructure (`Globe3D.jsx`'s `counts`, `ArLens.jsx`'s `lastReport`), 1 was a genuinely redundant import in `App.jsx` (the real `getRouteMeta` stayed correctly wired via `seoContext.jsx`, untouched). Verified: lint/typecheck/build clean after |
| 32 | Fixed bottom mobile nav (`MobileBottomTabs.jsx`, `z-[1000]`, global on all routes) overlapped `SiteFooter`'s last content — no page reserved clearance for it | Fixed | This session (2026-08-15) | — | Root cause: `SiteFooter` is a sibling *after* `</main>` on all 34 pages that render it, not the last child inside `<main>` — so `Campaign.jsx`'s existing `pb-24`-on-`<main>` pattern (copied as a first attempt) had zero effect, since scrolling to `document.body.scrollHeight` always places the true last DOM element flush against the viewport bottom regardless of padding earlier in the flow (proven via Playwright `getBoundingClientRect()` geometry checks, not assumed). Real fix: `pb-[calc(6rem+env(safe-area-inset-bottom))] lg:pb-0` added directly to `SiteFooter`'s own `<footer>` element — fixes all 34 pages at once from a single file. Verified with the "Open Command Center" button's real rendered position clearing the nav by ~144px at 320/375/390px viewports on `/support` and `/plans`. Regression coverage: `e2e/footer-nav-clearance.spec.ts` (verified to genuinely fail without the fix, not just pass with it) |
| 33 | `LeadClaim.jsonc`'s `read: {}` (public, unrestricted read of every claim + recon note) flagged for re-verification during a security audit | Verified clean — intentional | This session (2026-08-23) | — | Confirmed by cross-referencing `PortalOps.jsx`'s own ops-facing entity summary (`'LeadClaim', 'R/CREATE OPEN · U/D ADMIN'`) against the same pattern on `FundingLead`/`QuestCompletion` (RLS `create` locked to admin, but a server function does the real elevated write via `asServiceRole` — `claimLead/entry.ts`'s own comment states this explicitly). Public read matches the app's leaderboard-style operative/claim visibility model; no direct-write path exists for unauthenticated users. No code change needed |
| 34 | `LocationMap.jsx:229`'s `dangerouslySetInnerHTML={{ __html: thumbHTML(m) }}` flagged for XSS trace during a security audit | Verified clean | This session (2026-08-23) | — | `thumbHTML()` (`src/components/ooh/map/LocationThumb.jsx:49`) already runs every interpolated field (`m.image`, `m.title`) through a local `esc()` HTML-entity-escaper before building the string; the only unescaped value (`meta.label`) comes from a fixed internal `TYPE_META` map, never user input. No code change needed |
| 35 | `fetchMapLocations` has no live caller anywhere in `src/`, despite `PortalOps.jsx`'s own architecture diagram (`EDGES`) claiming it's reachable from `/campaign` | Fixed (doc) / Not built (functionality) | This session (2026-08-23, found) / this session (2026-08-24, root-caused) | — | Root-caused via `git log -S`, not guessed: the diagram was **always wrong** — the real (now-removed) call site was `src/pages/Map.jsx`, never `/campaign`. Found the exact removal: `base44.functions.invoke("fetchMapLocations", {pages:4})` was deleted from `Map.jsx` on 2026-07-24 (commit `d8c42a5`, 5 minutes after it was added in `ebd64c0`, both by the founder working directly in Base44's builder — this repo's git history shows only Base44's own auto-sync commits for that period, not this engineering workflow). `Map.jsx` now reads location data via `useLocations.js` → `base44.listAllLocations()` + a real-time `.subscribe()` — a direct entity read, strictly more capable than `fetchMapLocations`'s external-feed-scrape/WordPress-directory-scrape fallback chain. Conclusion: **stale documentation, not dead-and-should-be-deleted, not another legitimate path** — the function is real, orphaned by a legitimate architecture upgrade, and (per Base44's usual model) still directly invocable by URL regardless of frontend wiring, so it retains real exposure despite zero current legitimate traffic — already covered by this session's R-05 caching work (`fetchMapLocations` gets the same `IntelCache`-backed protection as the two functions that ARE still called). `PortalOps.jsx`'s wrong `['/campaign', 'fetchMapLocations']` edge removed. **Left for Dave, not decided here:** whether to keep this function as a fallback import path (e.g. if the WordPress feed needs re-scraping again) or delete it as dead code — genuinely a product call, not an engineering one, given it's already safe (cached, no live exposure risk beyond what any public function has) |
| 36 | 11 real public routes had no entry in `src/lib/routeMeta.js`'s `META` table, so they silently inherited the homepage's title/description in prerendered static HTML and client-side `document.title` | Fixed | This session (2026-08-24) | — | Cross-referenced `App.jsx`'s real route list against `routeMeta.js` directly (not assumed): `/access-keys` (+ its `:slug` sub-route), `/brand`, `/contact`, `/graffiti`, `/media-corps`, `/lab/coin-poster`, `/lab/devices`, `/lab/graffiti-cam`, `/lab/scanner`, `/lab/streetrunner`, `/lab/token` were all missing. Added accurate entries — lab-page copy reused verbatim from `LabHub.jsx`'s own existing `META` object for consistency rather than inventing new copy; the others written from each page's actual on-page heading/intro text. Verified: `npm run build` output grew from 57 to 68 prerendered routes (exactly the 11 added), spot-checked `dist/media-corps/index.html`, `dist/lab/streetrunner/index.html`, `dist/graffiti/index.html` for correct `<title>`/`og:title`. Also: `/graffiti` was missing from `public/sitemap.xml` despite sitting in the identical portal cluster as `/adbusting`/`/ecology`/`/rivers`/`/warzones` (all four of which are listed) — added. `public/robots.txt` allowed crawling of admin/internal-ops panels (`/lab/admin`, `/store/admin`, `/careers/admin`, `/portal/ops`, `/portal/investor`, `/portal/client`, `/dashboard`, `/account`) with no `Disallow` — added standard exclusions; these pages are auth-gated client-side anyway so this only affects search-engine crawl/indexing, not access. Verified: `e2e/route-metadata.spec.ts` still passes (3/3), `dist/robots.txt`/`dist/sitemap.xml` contain the new entries post-build |
| 37 | `AccountMenu.jsx`'s nav dropdown (guest and authenticated variants) closed on Escape/outside-click but never returned focus to the trigger — a keyboard user who opened it and closed it via Escape lost their focus position entirely (fell back to `document.body`) | Fixed | This session (2026-08-24) | — | Deliberately did **not** adopt `useFocusTrap`/`role="dialog"`/`aria-modal="true"` here — that hook is for genuine modal overlays (CommandCenter/NavMenu/QuickCapture/UnitFinder); applying it to a small anchored nav dropdown would incorrectly tell screen readers the whole page became inert. Instead: Escape now explicitly refocuses the trigger button (no other sensible focus target exists for a keyboard-only close). Outside-click deliberately does **not** refocus the trigger — a first implementation attempt did, and the new e2e test caught it as wrong: the user's click already has its own target (another link, empty space), so stealing focus back to the account button would fight the interaction they were actually making. Added `e2e/account-menu-focus-restore.spec.ts` (2 tests, one per case) — no prior coverage of this component existed. Verified: lint/typecheck/format/build clean, both new tests pass, existing `focus-trap.spec.ts` + `unit-finder-focus-trap.spec.ts` (6 tests, the components that DO use the dialog pattern) still pass unaffected |
| 38 | `aria-hidden-focus` on `/about`, `/report`, `/location/:id`, `/trash` (CommandCenter overlay) — re-baselined 2026-08-11 as "too large for a scope-locked commit," not actually fixed despite KNOWN_ISSUES #13 claiming it was | Fixed | This session (2026-08-29) | — | Root-caused precisely, not guessed: `CommandCenter.jsx` stays mounted and toggles `aria-hidden` rather than unmounting, and `aria-hidden` alone does not remove keyboard focusability from descendants — its close button and every nav link inside stayed tabbable while visually hidden. The WHATWG-recommended pairing is `inert`, but this app is on React 18, which does not support `inert` as a JSX prop — confirmed live before assuming a fix worked: `inert={!open}` on the element rendered no DOM attribute at all (`el.hasAttribute('inert') === false`). Fixed by setting `container.inert` imperatively inside the shared `useFocusTrap.js` hook (already used by `CommandCenter`/`NavMenu`/`QuickCapture`/`UnitFinder`), gated on the same `open` state the hook already tracks — safe for the other three consumers since they unmount when closed (`containerRef.current` is already null then, so the new branch never runs for them). Verified live against a real preview server: axe-core `aria-hidden-focus` count is 0 on all 5 baselined routes (was 1 on 4 of them), `e2e/focus-trap.spec.ts` + `e2e/unit-finder-focus-trap.spec.ts` + `e2e/account-menu-focus-restore.spec.ts` (11 tests) all pass serially — an initial parallel-worker run showed 2 unrelated failures (a `color-contrast` flake and a timing-sensitive focus-trap test) that both passed cleanly in isolation and serially, diagnosed as resource contention in this sandbox, not a real regression. `e2e/a11y-baseline.json` updated to drop the now-fixed entries rather than leave stale debt on the books |
| 39 | Initial-bundle weight never measured against what's actually eager vs. route-lazy | Fixed (partial) / Open (rest) | This session (2026-08-30) | `commandCenterContext.jsx` | Measured for the first time via `ANALYZE=true npm run build`'s `rollup-plugin-visualizer` treemap (already wired up, opt-in, unused until now) rather than guessing: parsed the emitted `dist/bundle-analysis.html` JSON to attribute every byte of the eager entry chunk (644KB uncompressed) to its actual source. Two real findings. **Fixed:** `CommandCenterProvider` (mounted unconditionally in `App.jsx`) statically imported `CommandCenter`, which statically imports `GraffitiCamera` (a device-camera capture UI) -- ~28KB of app code shipped to every page view though `openCommand` is only ever wired to explicit `onClick` handlers (Nav/HeroConsole/SiteFooter), never called on mount. Made `CommandCenter` `lazy()`, gated behind an `everOpened` flag so it mounts once on first open and then keeps its existing always-mounted/`inert`-toggling behavior (KNOWN_ISSUES #38) for the rest of the session. Verified: main chunk 644.57KB -> 616.88KB (gzip 208.10KB -> 200.12KB) via `npm run build` before/after; `focus-trap.spec.ts`, `command-center-overlay.spec.ts`, `footer-nav-clearance.spec.ts`, `a11y.spec.ts` (12 tests covering CommandCenter's open/close, focus-trap, and GraffitiCamera-stays-interactive paths) all pass unaffected. **Open, deliberately not touched:** the two largest remaining contributors to the eager chunk are `motion-dom`+`framer-motion` (~384KB uncompressed, the single largest slice, larger than `react-dom`) and the `socket.io-client`/`engine.io-client`/`partysocket` realtime stack (~123KB). Neither has a safe smallest-fix available: framer-motion's `AnimatePresence` wraps `<Routes>` in `App.jsx` itself and is load-bearing for the multi-second route-unmount timing this session's own react-query `staleTime` tests (#168, #169) depend on -- deferring it is a real architecture change, not a bounded fix. The socket stack is pulled in by `@base44/sdk@0.8.42` itself (`dist/client.js` -> `dist/utils/socket-utils.js` -> a static top-level `import { io } from "socket.io-client"`, unconditional regardless of whether any `.subscribe()` is ever called) -- vendored, not our import graph; a version bump risks unrelated breakage and wasn't attempted. Left open for a future session to size a version bump or evaluate deferring `AnimatePresence` deliberately, not silently |
