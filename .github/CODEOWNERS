# CODEOWNERS — RECOMMENDATION, NOT YET ACTIVE
#
# GitHub only enforces this file once real usernames/teams replace the
# placeholders below AND branch protection has "Require review from Code
# Owners" turned on (see BRANCHING_STRATEGY.md). We don't have a verified
# GitHub username for anyone but the repo owner, so this ships as a single
# catch-all rule pointing at whoever GitHub resolves the repo owner to be —
# swap in real handles before relying on this for review routing.
#
# Suggested shape once the team grows past one person: split by area so
# payment/treasury changes always get a second set of eyes even if the
# person who touched them is also a maintainer.

# Default: repo owner reviews everything not claimed below.
* @oohearth

# Example specialisation — uncomment and fill in once there's a second
# maintainer to assign:
# /base44/functions/stripeWebhook/   @<payments-owner>
# /base44/functions/cryptoWatch/     @<treasury-owner>
# /.github/                          @<engineering-owner>
