# Priority Queue

## P0-CANDIDATE — production function drift, one entry security-relevant, 2026-09-25 (NOT DEPLOYED, awaiting human authorization)

- **STATUS:** SOURCE-VERIFIED via live `functions pull`, NOT deployed. **Technical detail deliberately withheld from this public file** — see `04-SECURITY-QUEUE.md`'s redacted entry; full detail kept in private notes pending fix, per responsible-disclosure practice for a public repository.
- **WHAT (public summary only):** two production functions are running behind already-hardened, already-tested source that already exists on `origin/main` and is already live on BACKUP. One of the two is security-relevant.
- **OWNER:** unassigned, needs Dave's authorization to deploy
- **FIX:** redeploy already-tested, already-qualified `origin/main` source — no new code, commands prepared privately.
- **BLOCKER:** Lane D (production function deploy + security-relevant change) — explicitly out of autonomous authority per this pass's own mission brief. Needs Dave's go-ahead naming the exact command.
- **NEXT ACTION:** Dave authorizes the deploy, then standard pre/post pull-and-diff verification + a DevTools smoke check of the affected surface on production.


Ordered. Update this file when priorities change — do not rely on chat memory.

---

## P0 — Production locations missing/loading incorrectly (Home map + Map/Globe + mobile)

- **STATUS:** RESOLVED — DEPLOYED TO PRODUCTION AND VERIFIED, 2026-09-22
- **OWNER:** current session
- **EVIDENCE:** Root cause confirmed via live reproduction (401-vs-200 race directly captured); fix (retry + in-flight de-duplication) built, tested (21/21 unit tests, lint/typecheck/build clean), deployed to BACKUP and verified, then deployed to production and verified. `Location?...limit=500` request count dropped from 8+ to 1 (BACKUP) / 2 (production) per page load. Full gate-by-gate record in `02-INCIDENT-MOBILE-LOCATIONS.md`.
- **BLOCKER:** none — resolved.
- **NEXT ACTION:** none required. Monitor for recurrence; the fix reduces but does not mathematically guarantee zero chance of the underlying session-timing race (now protected by both fewer requests and a bounded retry).

## P0 (REOPENED, ROOT-CAUSED, CLOSED) — Globe markers never render (Home "Orbital Atlas" + Map default globe view)

- **STATUS:** CLOSED — DEPLOYED TO BACKUP AND PRODUCTION, LIVE-VERIFIED ON BOTH, 2026-09-23. Reopened by Dave's direct report ("still no show on that one... working on flat mode not globe mode") — confirmed as a real, general (not mobile-specific), fully independent defect from the P0 fix above.
- **OWNER:** current session
- **EVIDENCE:** Root cause confirmed via direct instrumentation (live `maplibregl.Map`/`GeoJSONSource` internal state) + network capture (`GET .../assets/maplibre-gl-worker.mjs` → 404) + maplibre-gl library source tracing. The GeoJSON source's worker actor never responds to any `setData()` call — `_isUpdatingWorker` stays `true` forever, `loaded()` never becomes `true`, no console error ever surfaces. Fixed via `maplibregl.setWorkerUrl()` + a `?worker&url` Vite import (a first `?url`-only attempt was insufficient — caught by the same direct-instrumentation technique before declaring victory). Full gate-by-gate record, including the discarded first attempt, in `02-INCIDENT-MOBILE-LOCATIONS.md`.
- **BLOCKER:** none — resolved.
- **GIT RECONCILIATION, 2026-09-25:** PR #274, all 10 CI checks green (CodeQL/Analyze included), squash-merged into `origin/main` at `0ed56883257f2ba684891b6380d21d70000e0adb`. No redeploy needed — the PR's base was already main's tip, so production's artifact (live since 2026-09-23) is unchanged.
- **POST-MERGE BROWSER VERIFICATION, 2026-09-25: PASS.** Chrome was unavailable in-session; resolved by launching Playwright's already-installed local Chromium headless with remote debugging, no system install/sudo needed. Real rendered-DOM/WebGL verification (not proxy signals) across Home + Map, globe + flat + both transitions, 3 mobile viewports (360×800/390×844/412×915) + 1 landscape smoke, Location Detail, and Slow 3G/Fast 3G network — all PASS with real markers visibly painted and geographically attached through rotation. Marker tap/click interaction was inconclusive via synthetic touch dispatch (tooling limitation, not a regression signal) — not independently re-confirmed this pass. Full record, screenshots list, and two new unrelated minor findings (image-404 gap; landscape globe container too short; lazy-chunk-preload-failure has no retry) in `02-INCIDENT-MOBILE-LOCATIONS.md`. `AUTOMATED_MOBILE_VISUAL_VERIFICATION: PASS` / `DAVE_PHYSICAL_DEVICE_CONFIRMATION: PENDING`.
- **NEXT ACTION:** none required to keep this P0 closed. `MediaCorpGlobe.jsx` (identical latent bug, not user-reported) was fixed proactively in the same PR. Ask Dave to retest on his own phone; the 3 new minor findings above are separate, non-blocking follow-ups.

## P0.1 → RECLASSIFIED P3 — Realtime WebSocket rejected for anonymous sessions (root cause CONFIRMED, 2026-09-22)

- **STATUS:** ROOT CAUSE CONFIRMED, HIGH CONFIDENCE. Reclassified from "P0.1" to **P3 / DEGRADED-BUT-FALLBACK-WORKS**. The "P0.1" label was historical (carried over from initial triage) and is not preserved here since the evidence no longer supports urgent severity — see `USER_IMPACT` below.
- **OWNER:** unassigned
- **ROOT CAUSE:** the Base44 realtime gateway (`wss://<domain>/ws-user-apps/socket.io/`, Socket.IO v4 over Engine.IO v4) explicitly rejects the Socket.IO-layer CONNECT handshake whenever the connection URL's `token` query param is the literal string `"null"` (i.e., anonymous/unauthenticated sessions). Engine.IO transport/handshake succeeds identically in both cases (open packet with `sid`/`upgrades`/`pingTimeout`/`pingInterval` received fine); the divergence is specifically the Socket.IO CONNECT step, ~1.3s after the socket opens: anonymous sessions get `44{"message":"Connection rejected by server"}` (Socket.IO CONNECT_ERROR) and the socket closes (code `1005`, no reconnect attempt ever fires — confirmed by a 19s observation window with zero further `WebSocket` constructions). Authenticated sessions (real JWT) complete `40{"sid":...}` CONNECT SUCCESS and proceed to normal entity-subscription `"join"` events.
- **CONFIRMED BY A CONTROLLED A/B TEST**, reproduced identically on both environments:
  - Production, anonymous, isolated context (`p01-realtime-prod`): rejected.
  - BACKUP, anonymous, isolated context (`p01-realtime-backup-anon`, fresh cookie jar, 2026-09-22): rejected, byte-identical signature (`token=null` → open → `40` sent → `44{"message":"Connection rejected by server"}` at t=2192ms → close code 1005 at t=2806ms, zero reconnect in 19s).
  - BACKUP, authenticated (real JWT, isolated context `isolated-context-2`): succeeds cleanly, `"join"` events proceed.
- **CONNECTION OWNER:** OOH Earth's own `src/hooks/useLocations.js` (and 14 other call sites — see below) call `base44.entities.<Entity>.subscribe()` unconditionally, with no auth-state gating (`useAuth()`'s `isAuthenticated` is never checked before subscribing). The socket itself and its accept/reject decision are Base44 platform code (`@base44/sdk`'s internal realtime client + the platform's own gateway) — OOH Earth code initiates the call but does not control the accept/reject decision.
- **FIX OWNERSHIP: C (Base44 platform)** for the actual defect (anonymous sessions cannot open a realtime channel at all — this is a server-side accept/reject policy, not something app code can override). A narrow **mitigation** (not a fix) is available and IS OOH-Earth-owned: gate the ~15 `.subscribe()` call sites behind `isAuthenticated` to stop attempting (and silently failing) a channel the platform will never accept for anonymous users. **Not implemented this session** — see `NEXT ACTION`.
- **CONNECTION FAN-OUT:** exactly 1 physical WebSocket per page load (not duplicated at the transport level), but on a page that mounted multiple subscribing components, each of 5 entity types (`Location`, `Operative`, `DigitalBust`, `LeadClaim`, `FundingLead`) issued 2 `"join"` messages on one socket — confirming no shared/dedup layer exists over the 15 independent `.subscribe()` call sites (`Leaderboard.jsx`, `LiveActivityFeed.jsx` ×4, `OperativeUnitRoster.jsx`, `useGamification.js`, `useLocations.js`, `Dashboard.jsx`, `FdePortal.jsx`, `InHome.jsx`, `Map.jsx` ×2, `AdbustingPortal.jsx`, `GraffitiPortal.jsx`) — the same uncoordinated-multi-caller architecture pattern that caused the original P0 REST-fetch bug. Exact component-pair attribution for the observed 2x was not isolated further (would need per-call-site stack tracing); not required to characterize user impact.
- **USER IMPACT: DEGRADED-BUT-FALLBACK-WORKS, low severity.** Anonymous visitors (the large majority of traffic) never receive realtime push updates (new Location markers, live activity feed events) without a manual refresh — data is fetched correctly via the already-fixed REST path (`listAllLocations()`) on load, just not pushed live. Authenticated users are unaffected (their socket connects and subscribes normally). No error loop, no retry storm (confirmed: zero reconnect attempts observed), no user-visible error surface (the browser console shows a `connect_error`, but the app never surfaces it in UI). This does **not** regress or relate to the P0 fix (separate code path, separate root cause — proven, not assumed).
- **P0 CORRELATION:** DISPROVEN as the same root cause. Same *architectural pattern* (many uncoordinated callers, no shared layer) but a completely different failure mechanism (REST 401 timing race vs. WebSocket anonymous-rejection policy) and a completely different fix shape.
- **P0.2 CORRELATION:** DISPROVEN. P0.2 (`429` on `/api/runtime/session-recordings/ingest`) is a different endpoint, different protocol (HTTP vs. WebSocket), different Base44 subsystem (session-recording telemetry vs. realtime pub/sub) — no code or evidence links them.
- **STATUS UPGRADED, 2026-09-22: DEPLOYED TO PRODUCTION, VERIFIED, GIT-RECONCILED.** All of the below now applies to production, not just BACKUP.
- **BLOCKER:** none — the platform-level accept/reject policy itself still cannot be changed from this repo (unchanged, and this mitigation never claimed to change it).
- **MITIGATION IMPLEMENTED, 2026-09-22:** a fresh source search found 21 individual `.subscribe()` calls across 14 files (not 15 — the earlier count missed 6 sites using the `?.subscribe?.()` optional-chaining form: `FieldCheckPanel.jsx`, `NavMenu.jsx`, `PublicSpacePanel.jsx`, and 3 more inside `Dashboard.jsx`'s single combined effect). Built a shared hook, `src/hooks/useAuthGatedSubscribe(entityName, callback)`, backed by a pure, dependency-free core (`src/hooks/authGatedSubscribeCore.js`: `shouldOpenSubscription`/`subscribeToEntity`, unit-tested) — it subscribes only once `authChecked && isAuthenticated` are both true, using a `useRef`-held callback so re-renders never tear down/recreate the subscription. All 14 call sites now go through this hook instead of calling `base44.entities.<Entity>.subscribe()` directly; each site's own REST fetch effect was left untouched (split out where fetch and subscribe previously shared one effect). See `06-EVIDENCE-LOG.md` for the full file-by-file record and `07-DECISION-LOG.md` for the architecture-choice tradeoff (centralized hook over per-site checks) and the specific per-file handling decisions.
- **VERIFIED LIVE ON BACKUP, then PRODUCTION, 2026-09-22:** anonymous sessions (desktop + 390×844 mobile) show **zero** WebSocket construction attempts (confirmed via a `WebSocket`-constructor monkey-patch, same technique as the original P0.1 capture) and **zero** `connect_error` in console, on both environments, while `Location`/etc. REST loading is unaffected (production: 939 live spots / 783 verified rendering correctly, P0 dedup intact). An authenticated session (real JWT, on BACKUP) still opens the socket and reaches Socket.IO CONNECT SUCCESS normally — **not independently re-verified on production** (no pre-existing authenticated production browser session was available; none was manufactured, per the authorization's explicit instruction). `Dashboard.jsx` (the highest-complexity touched file, 4 subscribe sites) renders cleanly authenticated with zero console errors on BACKUP.
- **DEPLOYMENT RECORD:** BACKUP first (`base44 site deploy --app-id 6a6748e009b947cb29591871`), caught and corrected a self-inflicted deploy-config bug in the process (missing `VITE_BASE44_APP_ID`, see `07-DECISION-LOG.md`); then production, under explicit executive authorization, via `base44 site deploy --no-build --yes --app-id 6a62213cff3ccbca88c04ff5` with `VITE_BASE44_APP_ID` explicitly set and the built artifact's app-id content proven (8 occurrences of production's id, 0 of BACKUP's, in the true entry file referenced by `dist/index.html`) before deploying. Both deploys used the narrow `site deploy` verb only — never the broad `deploy`, never entities/functions.
- **GIT RECONCILIATION:** PR #269 (`fix(realtime): skip Base44 entity subscribe attempts for anonymous sessions`) — all 10 required CI checks passed (Lint & Typecheck, Build, Playwright smoke+accessibility, Playwright mobile Chromium, Prettier, Dependency audit, Dependency Review, CodeQL/Analyze, PR summary comment), squash-merged into `origin/main` at `38da7f63983f3254012fa93fdb059f6eed209bc7`. Post-merge drift check confirmed empty — the already-deployed production artifact needed no redeploy.
- **NEXT ACTION:** none. Fully closed.

## P0.2 → CLOSED / PLATFORM TELEMETRY — analytics/session-recording 429 (root cause CONFIRMED, 2026-09-22)

- **STATUS:** ROOT CAUSE CONFIRMED, HIGH CONFIDENCE. Closed as platform telemetry, not preserved at its historical "P0.2" name/severity — evidence shows zero product impact and zero fix surface in this repository.
- **OWNER:** n/a (fully platform-owned, see below)
- **ROOT CAUSE:** production's served HTML (`oohearth.app` only) contains an inline bootstrap `<script>` that is **not present anywhere in this git repository** (confirmed: `git grep` for `rrweb`/session-recording/analytics/telemetry-init across `origin/main` finds only unrelated UI copy and the app's own small `src/lib/trackEvent.js`/`analytics/track/batch` wrapper — neither is this script). The injected script: sets up `window.Base44.recordings`, dynamically loads `rrweb@2.0.0-alpha.20` from `unpkg.com` (SRI-pinned, `crossOrigin: anonymous`), and — at `SAMPLING_RATE = 1.0` (100% of sessions attempted) — POSTs a full DOM-snapshot session-recording payload (~240KB observed) to `POST /api/runtime/session-recordings/ingest` using a Base44-issued `x-base44-recording-token`. Production's workspace has exhausted its session-recording quota: the response is **not a rate limit** (no `Retry-After` header) but a hard quota rejection — `429`, body `{"message":"Recording limit reached"}`, response header `x-base44-recording-limit: reached`.
- **CONFIRMED BY DIRECT INSPECTION**, not inferred: read the actual inline script content via `evaluate_script` (found `window.Base44`/`rrweb` globals, the CDN `<script src="https://unpkg.com/rrweb@2.0.0-alpha.20/...">` tag, and the full bootstrap source including the ingest token, endpoint, and session-limit constants `MAX_SESSION_MS=1800000`, `IDLE_TIMEOUT_MS=900000`). Live-captured the actual `429` response headers/body via `get_network_request` (reqid 970, then reproduced again on a fresh hard-navigation to `/map`, reqid 1202 — same signature both times).
- **ENVIRONMENT ASYMMETRY (new finding, unlike P0.1 which was identical everywhere):** BACKUP (`ooh-earth-backup.base44.app`) has **no recording bootstrap script at all** — confirmed via the same `evaluate_script` global/script-tag check, zero `window.Base44`, zero `rrweb`, zero `session-recordings/ingest` request across a full page-load network capture. The feature is injected per-app/workspace (production only), not a universal Base44 behavior — this repo's own code is identical on both, so the difference is entirely platform-side configuration.
- **REQUEST VOLUME:** exactly 1 attempt per full page load (confirmed on 2 separate hard navigations, `/` and `/map`); 0 additional attempts observed during ~30s+ of subsequent in-page activity (map pan/zoom, `fieldStats`/`fieldNews`/`InvokeLLM` calls, air-quality lookups) — this is a session-scoped recorder, not a per-route or per-idle-tick one. **No retry loop** — the rejection is immediate and final for that session; nothing in the captured traffic showed a second attempt.
- **USER_IMPACT: TELEMETRY-ONLY.** The failure is silent (no console-visible app error tied to it beyond the network-panel 429 itself), blocks nothing, and is unrelated to every product surface checked (Home/Map/Globe/Location Detail/Account/REST data loading/realtime/moderation/Founding Profiles/Public Space all function identically with or without this request succeeding).
- **PERFORMANCE_IMPACT: minor, bounded, one-time.** ~240KB DOM-snapshot serialization + upload once per full page load, discarded server-side — comparable to one extra medium image request, not a request storm, not a CPU loop.
- **P0_CORRELATION / REALTIME_CORRELATION:** both disproven — different endpoint (`/api/runtime/session-recordings/ingest` vs. Location REST vs. the Socket.IO realtime gateway), different Base44 subsystem, zero shared code path.
- **FIX_OWNERSHIP: C (Base44 platform), unambiguous.** The responsible script is injected server-side into production's HTML outright — it does not exist in this repository's source, build config, or dependency tree in any form (`rrweb` is not in `package.json`; the bootstrap is not in `@base44/vite-plugin`'s own injection list either — it is injected by a separate, external Base44 hosting-layer mechanism this repo has no visibility or control over). There is no application-level opt-out, sampling control, or configuration surface available to OOH Earth's own code.
- **BLOCKER:** none — this is a workspace-level Base44 plan/quota matter (session-recording quota exhausted), not a code defect. Resolving it (if ever wanted) would mean a workspace/billing action with Base44, not an engineering task.
- **NEXT ACTION:** none required from this repository. Optional: if the ~240KB-per-load waste is ever judged worth eliminating, that is a Base44 support/workspace-settings conversation (e.g., ask Base44 to disable session recording for this app, or raise the quota), not a code change here.

## LANE A — Vetted, ready-to-merge PRs, BLOCKED by auto-mode classifier ("Merge Without Review"), 2026-09-25

- **STATUS:** Reviewed (CI, mergeability, diff content) and judged safe for a squash-merge; the merge attempt itself was denied by this session's sandbox classifier. Per established standing practice in this engagement, a classifier denial is a stop-and-report checkpoint, not something to route around — not retried, not attempted via any other tool/method.
- **VETTED AND READY (human can merge directly, single command each: `gh pr merge <n> --squash --delete-branch`):**
  - #211 postcss-selector-parser 6.1.2→6.1.4 (patch, CI green)
  - #214 @humanfs/node 0.16.7→0.16.8 (patch, CI green)
  - #217 fflate 0.8.2→0.8.3 (patch, CI green)
  - #248 js-yaml 4.3.1→4.3.2 (patch, CI green)
  - #249 dev-dependencies group (6 updates: @playwright/test, autoprefixer, baseline-browser-mapping, deno, eslint-plugin-react-refresh, postcss — all minor/patch, CI green)
  - #265 npm-routine group (9 updates incl. @base44/sdk 0.8.42→0.8.48, maplibre-gl 6.7.0→6.9.0, @tanstack/react-query, react-router-dom, three — all minor/patch, CI green including the Playwright globe-markers spec, so the maplibre-gl bump doesn't regress the P0 globe fix)
  - #192 GitHub Actions routine bump (codeql-action init/analyze, CI green)
  - #190 rollup-plugin-visualizer 6.0.11→7.1.1 (major, but dev-only build-analysis tool, zero runtime impact, CI green)
  - #188 framer-motion 12.43.0→13.2.0 (major, CI green across lint/typecheck/build/Playwright smoke+a11y+mobile — a real UI-runtime library, so this is a reasonable evidence bar, not blind trust, but flagged as the highest-risk item in this batch)
  - #189 react-resizable-panels 2.1.9→4.12.4 (major, CI green)
  - #105 `fix(a11y): expand footer link touch targets to 24px on mobile` — real fix, new Playwright regression test, verified pixel-identical layout (padding+matching negative margin)
  - #108 `feat(protocol-one): make the shareable Protocol One page discoverable` — adds a footer link + sitemap.xml entry for `protocol-one.html`, which previously had zero inbound links; new Playwright coverage
- **HELD BACK, NOT VETTED FOR AUTO-MERGE:**
  - #156 `feat: add attributable live-use paths to README` — otherwise clean, but changes the public contact email (`hello@outofhell.org` → `oohearth@proton.me`) as a side effect. That's a real identity/ownership change, not mechanical — needs Dave's explicit confirmation that the new address is correct and monitored before merging, not an autonomous call.
  - #20/#39/#88 (react-leaflet 4→5, react-dom bump, react bump) — all 3 fail identically on Lint & Typecheck, Prettier, Build, and Dependency audit. Needs real investigation of what specifically breaks (likely a react-leaflet v5 API/breaking-change interaction), not a blind retry or override.
  - #191 (typescript 5.9.3→7.0.2) — fails Lint & Typecheck. A very large major-version jump for TypeScript; needs investigation before considering.
  - #254 `lynxie/fix-executor-trusted-checkout` — CI green, but touches a CI/CD publish-executor's trust boundary (manifest/patch checkout isolation) — security-adjacent infrastructure change, judged to need a real read-through before merging, not just a green-CI nod.
  - #63 release-please `chore(main): release 1.3.0` — `BLOCKED`, matches the already-known, already-tracked `release-please CI gap` (see `[[project_release_please_ci_gap]]` memory / PR #64, still unmerged) — required checks never run due to the anti-recursion `GITHUB_TOKEN` gap. Not a new finding, no new action here.
  - #201 `codex/evidence-review-conversion-route` — still a DRAFT, not ready for merge consideration.
  - #152/#153/#154/#158 (`rnd/*` research branches) — each PR's own title says "not for merge" / "negative result" / "R&D, not for merge yet". Lane F — document, do not ship, regardless of CI status.
- **NEXT ACTION:** Dave (or anyone with merge permission) runs `gh pr merge <n> --squash --delete-branch` for the 12 vetted PRs above whenever convenient — no further engineering review needed for those. The "held back" set each needs a specific human answer or investigation before it's mergeable.

## P1 — Verify multi-ad/multi-brand behavior for one physical Location

- **STATUS:** CONCLUDED — definitive answer from schema + code, no fix needed/proposed
- **OWNER:** current session
- **EVIDENCE:** Dave's reference location `6ab1fc4a69f030c03ab1c6b9` (Samsung Electronics + Toyota Motor Corp on one billboard). Full findings in `02-INCIDENT-MOBILE-LOCATIONS.md`: `Location.parent_corp`/`brand_name` are singular fields; multi-brand representation is a manual human workaround (string concatenation), not a system feature; no automated overwrite risk found; not connected to the P0 incident.
- **BLOCKER:** none — concluded.
- **NEXT ACTION:** none required unless a product decision is wanted on formalizing multi-brand support (out of scope, not requested).

## COMPLETE — Production permission hardening (User.role/access/agency, Location.status, FieldCheck.status)

- **STATUS:** DEPLOYED AND SOURCE-VERIFIED, 2026-09-22. Gates 1-6 all passed. `entities push` (human-executed 2026-09-22) succeeded; confirmed via the authoritative `GET /api/apps/{id}/entity-schemas` API — all 23 entities match the deployment mirror exactly, exactly `User`/`Location`/`FieldCheck` changed vs. pre-push, 20 unchanged.
- **OWNER:** n/a — closed
- **EVIDENCE:** `06-EVIDENCE-LOG.md` (2026-09-22 "Gate 6 correction" entry); `ROLLBACK_entities_20260922/POST_PUSH_authoritative_entity-schemas_response_20260922.json` has the raw authoritative response.
- **IMPORTANT CAVEAT:** an initial verification pass via the Base44 Code-tab/Monaco editor wrongly showed no change and was briefly reported as a failed push — that display is NOT a reliable proxy for the runtime schema store. Corrected via direct API query. See `06-EVIDENCE-LOG.md` for the full trail and the mandatory process change (always verify via `entity-schemas` API directly, never the Code tab).
- **NOT YET DONE:** RUNTIME-VERIFIED behavioral confirmation (an actual non-admin write attempt) — remains out of bounds without separate explicit authorization for a synthetic production test (Gate 7).

## COMPLETE — LeadClaim open-create + missing note cap

- **STATUS:** CLOSED — SOURCE/SCHEMA VERIFIED, 2026-09-22. Fix deployed and confirmed via the authoritative `entity-schemas` API. Not runtime/behaviorally tested (no synthetic claim submitted).
- **OWNER:** n/a — closed
- **EVIDENCE:** `04-SECURITY-QUEUE.md`/`06-EVIDENCE-LOG.md` 2026-09-22 entries have the full trail (initial push attempt that didn't execute, correction, mandatory preflight, execution, authoritative post-push diff, DevTools smoke check).
- **NOTE:** the first push attempt targeted a bare, unlinked directory and the CLI correctly refused before any write — zero risk, zero production impact. The successful push used the proven `prod-prerelease-patches/` worktree.

## COMPLETE — Public Space + Founding Profiles production feature release

- **STATUS:** DEPLOYED AND VERIFIED, 2026-09-22. All 3 writes executed under explicit executive authorization, each independently verified via the authoritative `entity-schemas` API / function pull, plus a clean DevTools smoke check across every affected surface.
- **OWNER:** n/a — closed
- **WHAT SHIPPED:**
  1. `entities push` — `Location` gained `client_operation_id`/`public_access`/`setting`/`status_updated_at` + 3 new `type` enum values (`skatepark`/`basketball_court`/`multi_use_court`); `FieldCheck` gained `client_operation_id` + the same 3 `location_type` enum values; `LocationRelationship` created as a new entity. Verified: exactly these changed/new, 21 entities untouched, 0 deleted, Phase 2 + LeadClaim locks intact.
  2. `functions deploy getPublicProfile` — new function, now live. Verified via `OPTIONS` (204) and a live nonexistent-handle check (`{"found":false}`, no leak) — both RUNTIME-VERIFIED. Also confirmed end-to-end via the real `/founders/:handle` page correctly rendering "PROFILE NOT FOUND".
  3. `functions deploy moderate` — discovered during deployment that the CLI's entry-file convention (`entry.{js,ts}`) meant the hand-built `main.ts` patch would have been silently ignored; deployed the git-tracked `entry.ts` instead (origin/main's current logic, already proven live on BACKUP) — functionally identical to the intended patch, verified byte-identical to origin/main post-deploy.
- **Frontend:** no deploy was needed — confirmed pre-existing since commit `c253b6a`.
- **EVIDENCE:** full write-by-write record in `06-EVIDENCE-LOG.md`'s "release executed" entry.

## CLOSED — full_name persistence defect, CONFIRMED, FIXED, SHIPPED, 2026-09-22

- **STATUS:** FULLY CLOSED. Root-caused, fixed, BACKUP-verified, deployed to production under explicit executive authorization, live-verified, PR #270 merged (`78286b2`), post-merge drift check empty.
- **PRIOR LABEL:** "OBSERVED, NOT INVESTIGATED" — now superseded with hard evidence.
- **ROOT CAUSE, confirmed via live reproduction on BACKUP (authenticated session, `adil16smy@gmail.com`):** Base44's own `PUT /api/apps/{id}/entities/User/me` silently ignores a changed `full_name` — the request correctly sends the new value, the response is `200`, but the response body (and an immediate follow-up `GET .../User/me`) both echo back the *old* value. `full_name` does not appear in `User.jsonc`'s custom `properties` at all, consistent with it being a platform-reserved, identity-provider-synced field (likely mirrored from Google OAuth at login) that the generic entity-update endpoint won't overwrite — a Base44 platform characteristic, not an app-level permissions bug.
- **THE ACTUAL APP-LEVEL DEFECT** (this part IS this repo's to fix, and was fixed): `src/pages/Account.jsx`'s `saveProfile()` called `setMe(u)` with the fresh post-save record but never `hydrate(u)` (the function that re-syncs the editable `form` state from a `User` record) — so the form, and the page heading that reads from it, kept displaying whatever the user had just locally typed, right next to a "Saved ✓" confirmation, even though the platform had silently discarded that specific change. A user changing their display name would see it "work" until their next page load, when it would silently revert with no explanation ever shown.
- **FIX:** one call added — `hydrate(u);` right after `setMe(u);` in `saveProfile`. This is a general correctness fix (the form always reflects server truth after any save, for any field), not a `full_name`-specific patch, and required no schema change, no new UI copy, and no product decision.
- **VERIFIED LIVE ON BACKUP, 2026-09-22:** (1) changed `full_name` → save → heading/field correctly revert to the true, unpersisted value instead of showing the fake typed one; (2) changed `region` (a normal, non-reserved field) → save → correctly persists and displays, confirming no regression to the happy path. Cleanup: BACKUP's test user's `region` was reset to empty afterward; `full_name` was never actually changed server-side at any point (the platform rejected every attempt), so nothing there needed reverting.
- **QUALIFICATION:** `npx eslint`, `npm run typecheck`, `npx prettier --check` all clean on the changed file; `npm run build` clean; the one existing Account-adjacent Playwright spec (`account-menu-focus-restore.spec.ts`, unrelated to the save flow but the closest existing coverage) passes 2/2.
- **NOT FIXED (out of scope, platform-owned):** the underlying reason `full_name` itself can't be changed via this endpoint. If a genuinely editable "display name" independent of the Google-synced identity is wanted, that requires a **product decision** (add a new custom `display_name` field to `User.jsonc` and migrate the UI to use it instead of `full_name`?) — not implemented, not decided, flagged for Dave.
- **DEPLOYMENT STATUS:** deployed to production (`base44 site deploy --app-id 6a62213cff3ccbca88c04ff5`, app-id hard-gate proven before deploy), live-verified (artifact content-hash match, `/account` boundary intact, no new console errors). PR #270 opened, all 10 CI checks passed, squash-merged into `origin/main` at `78286b2ecb7ef259dfb2837e2b75beb731b7d834`. Post-merge drift check (`git diff origin/main HEAD`) empty — no redeploy needed.
- **NEXT ACTION:** human decision on whether to pursue the separate, larger "genuinely editable display name" feature (new custom field, product decision). The sync-honesty fix itself needs no further action.

## CLOSED — Minor map-search accessibility label — ALREADY FIXED (stale finding), 2026-09-22

- **STATUS:** ALREADY COMPLETE. Re-checked current source rather than trusting the historical note.
- **EVIDENCE:** `src/components/ooh/map/MapSearch.jsx` — the search `<input>` already has `aria-label="Search map"` (and its clear button has `aria-label="Clear search"`). This is the only search-like input found on the Map page and Home (same component, shared `placeholder="Search street, city, or place"`).
- **CONCLUSION:** the label was added at some point after the original BACKUP regression-pass observation; the finding no longer describes current reality. No fix needed.
- **NEXT ACTION:** none.

## CLOSED — Public Space submission UI activation — ALREADY COMPLETE (stale finding), 2026-09-22

- **STATUS:** ALREADY COMPLETE. The 08-SESSION-HANDOFF.md note ("the types are schema-valid now but still not user-selectable, deliberately") is stale and does not describe current source.
- **EVIDENCE, traced end-to-end:** `src/components/ooh/report/ReportStep1Document.jsx` (the field-report wizard's step 1, rendered by the live `FieldReport.jsx` at `step === 1`) already lists `skatepark`/`basketball_court`/`multi_use_court` as selectable "Media type" buttons alongside the OOH-ad types. Selecting one conditionally reveals a "Setting"/"Public Access" panel (`isPublicSpaceType(data.type)` from `src/lib/publicSpace.js`), and both fields flow directly into the final submission payload (`FieldReport.jsx` lines ~172-173: `setting: data.setting, public_access: data.public_access`). Downstream rendering is also already wired: `LocationThumb.jsx`/`pinGlyphs.js`/`markerUtils.js` all recognize the 3 types for map markers, `DynamicFilterBar.jsx` filters by them, `categories.js` + the `/category/:slug` route (`App.jsx`) give each type its own directory page, and `LocationEditPanel.jsx` lets an operator retroactively set the type on an existing record.
- **CONCLUSION:** the submission → moderation (via the already-shipped `LocationRelationship`/`moderate` support) → map → category-page → detail-page flow is fully wired for all 3 facility types. No frontend work is needed to "activate" them — they were already user-selectable by the time this queue refresh ran.
- **NEXT ACTION:** none required. If real submissions exist, a live spot-check of one going through moderation would be a cheap, non-urgent confidence check — not scheduled.

## CLOSED (as a decision package) — Founding Profile discovery, full investigation delivered, 2026-09-22

- **STATUS:** DECISION PACKAGE COMPLETE. Full deep-dive in `10-FOUNDING-PROFILE-DISCOVERY.md`. Zero implementation, zero writes — **decision: DEFER**, not undecided.
- **CORE FLOW CONFIRMED COMPLETE** (unchanged from the prior finding): `Account.jsx`'s publish/unpublish toggle, handle validation/duplicate-check, and `FounderProfile.jsx`'s public rendering are all well-built and working.
- **DISCOVERY CONFIRMED URL-ONLY**, via a fresh complete search (not reused from prior notes): exactly one internal link exists anywhere in the app (the owner's own Account-page link). No leaderboard/feed/search/nav link exists. `Leaderboard.jsx` — the most natural alternative discovery surface — **deliberately masks all member identity today, for everyone**, a real existing privacy decision that weighs against extending discovery through it.
- **ADOPTION COUNT NOT SAFELY DETERMINABLE**: even the CLI's own elevated management token is refused for bulk `User` reads, confirming `getPublicProfile`'s "no general User.filter() for non-admins" principle is enforced platform-wide, not just client-side. Reported honestly as unknown rather than guessed. Strong qualitative signal instead: `profile_public` is referenced in exactly one place in the whole frontend, zero promotion/incentive anywhere — very unlikely to have meaningful uptake yet.
- **`getPublicProfile` confirmed structurally unable to support a directory** (resolves exactly one handle at a time, by design) — any directory needs a genuinely new, separately-consented, paginated, service-role listing function. Not built.
- **DECISION: DEFER.** Not a design failure — the target design (a clearly-labeled, separately-consented directory, preferred over extending the leaderboard) is sound, but building it now would produce an empty, recognition-undermining surface given the near-zero adoption signal. Mirrors the multi-brand decision's own reasoning: sound target, correctly not rushed.
- **RECONSIDERATION TRIGGERS** (see `10-FOUNDING-PROFILE-DISCOVERY.md` §13): Dave checks the real `profile_public=true` count via Base44's own dashboard and finds meaningful adoption; an organic user request for discoverability surfaces; or an unrelated future need for bulk public-User reads emerges.
- **NEXT ACTION:** none required unless a reconsideration trigger fires.

## CLOSED (as a decision package) — Multi-brand / ad-observation data model, full architecture investigation delivered, 2026-09-22

- **STATUS:** ARCHITECTURE DECISION PACKAGE COMPLETE. Full deep-dive in `09-MULTI-BRAND-DATA-MODEL.md`. Zero implementation, zero writes (schema/BACKUP/production) — this entry is now a decision awaiting Dave's answer, not an open investigation.
- **RECONSTRUCTED, not assumed:** `Location`'s physical-asset fields (`type`/`address`/`lat`/`lng`/`access_key`/`condition`/`ooh_operator`) vs. its time-varying commercial fields (`brand_name`/`parent_corp`/`ad_agency`/`campaign_name`/`industry_sector`/`harm_tags`/`adbust_type`) are conflated in one record with one lifecycle. Full 124-reference consumer matrix traced across submission/editing/display/search/gamification/moderation.
- **KEY FINDING:** `LocationRelationship` (shipped this session, 0 real rows yet) already IS a proven, working, moderated 1:N claims entity with exactly the shape a brand-observation model needs — and `PublicSpacePanel.jsx`'s own UI copy already distinguishes "raw visible branding" (unverified) from "a claimed relationship" (moderated) — the exact epistemic split this whole question is about. Verifying a `Location`/`FieldCheck`/`LocationRelationship` are already independent today (confirmed by reading `moderate/entry.ts`) — the "don't cascade moderation" requirement is already solved, not something new to build.
- **PRODUCTION DATA (read-only, 783 verified Locations):** only 1.9% have `brand_name` at all; exactly 1 record shows the actual multi-brand-concatenation symptom (`"Leo Beer / Singha Sparkling Water"`). `FieldCheck` has 0 verified rows in production — its brand-change-detection logic is code-correct but unexercised. **This is a real but currently rare edge case, not a systemic problem.**
- **RECOMMENDED MODEL:** a new `AdObservation` entity, structurally modeled on `LocationRelationship`'s proven shape, kept in its own domain (extending `LocationRelationship` itself was evaluated and rejected — its `relationship_type` enum is about durable facility relationships like sponsorship, not rotating ad placements; overloading it would blur a distinction the product already correctly draws). Fully additive: `Location.brand_name` et al. stay as a compatibility cache, zero forced migration/backfill.
- **RECOMMENDED URGENCY: LOW**, explicitly, given the production-data prevalence above — this is the honest, evidence-based recommendation, not a default to build immediately.
- **PRODUCT DECISIONS SURFACED (see `09-MULTI-BRAND-DATA-MODEL.md` §16), not silently decided:** (1) timing/priority — build now vs. wait for a real volume signal; (2) whether `ooh_operator` should be reclassified as physical-asset data (naming/documentation only); (3) whether `Location.brand_name` should eventually become read-only/derived once `AdObservation` exists, or stay an admin override; (4) whether to extend `advertiserRegistry.js`'s suggestion list to cover brand names, not just parent corps.
- **NEXT ACTION:** await Dave's answer on the decisions above. If he approves the recommended direction and priority, the phased delivery plan in `09-MULTI-BRAND-DATA-MODEL.md` §14 (Phase 0 → 5, each independently stoppable, each following the standard release runbook) becomes its own properly-scoped implementation task — not something to fold into a general queue-refresh or execution-loop session.

## IN PROGRESS — KNOWN_ISSUES.md #16, react-query migration (10/~16 pages done), 2026-09-22

- **STATUS:** advancing incrementally, one bounded PR per page, autonomous-release-eligible when clean.
- **DONE (10 files):** `BusStopDetail`, `FieldId`, `LocationDetail`, `StoreAdmin`, `Careers`/`CareerRole` (shared `useCareerRoles` hook), `LabHub` — all pre-existing before this session's queue-refresh pass. `MediaCorps.jsx` (PR #271, merged `262ab37`) and `AtariPortfolio.jsx` (PR #272, merged `0850422`) — added earlier this session. **`Store.jsx`'s catalog + owned-purchases read — FULLY SHIPPED, 2026-09-22 (PR #273, merged `9964d11`).** The load path (`storeCatalog` fn invoke + `auth.me()`/`Purchase.filter`) had zero writes in it and was already structurally separate from `onAction`/`deliver`/checkout, so it migrated cleanly to two independent `useQuery` calls with no touch to any payment/delivery code. BACKUP-verified, then **production-deployed and live-verified under explicit human authorization** (the sandbox's auto-mode classifier had blocked the first attempt inside the autonomous pass; a human re-issued the identical authorization and the sandbox permitted it on retry). New Playwright coverage: `e2e/store-catalog.spec.ts` (2 tests, anonymous + authenticated).
- **RESCOPED THIS PASS WITH FRESH FULL SOURCE READS (not reused from earlier informal notes) — all 3 confirmed DEFER, not "not yet attempted":**
  - `CareersAdmin.jsx` — confirmed via full fresh read: `load()` is read → conditional-write (`CareerRoleStatus.create()` for any role missing from the `ROLES` registry) → reread, the identical auto-provisioning hard-stop signature as `LabAdmin.jsx`. An automatic react-query refetch/retry could re-trigger the provisioning writes. **DEFER.**
  - `Plans.jsx` — confirmed via full fresh read: the load effect runs a manual `for` loop with `await new Promise((r) => setTimeout(r, 2500))`, up to 4 iterations, gated on `?status=subscribed`, polling for a webhook-landed `Subscription` row after checkout. This stateful, sequential retry pattern would fight (or have to reimplement) react-query's own retry/refetch semantics — not a small, clearly-correct separation. **DEFER.**
  - `PortalOps.jsx` — confirmed via full fresh read: one effect fires 5 independent reads (`opsIntel`, `fieldStats`, `cryptoWatch`, pending-`Location` queue, bulk geo `Location`+`FieldCheck`); the `queue` state specifically is written by BOTH that effect AND `verify()`'s inline optimistic `setQueue((q) => (q||[]).filter(...))` after a `moderate` call — a query-owned value mutated directly by a mutation handler, not via invalidation. 2211 lines, zero existing test coverage, moderation-critical surface. A bounded migration of just the other 4 reads would still mean restructuring one giant effect while leaving `queue`/`verify` untouched — not a small, isolated change on this surface. **DEFER.**
  - `Map`, `Dashboard`, `FdePortal`, `Account`, `InHome`, `portals/AdbustingPortal`, `portals/GraffitiPortal` — unchanged from the prior note: touched by other fixes earlier this session, deliberately left alone to keep each PR to one coherent concern, not unsafe.
- **NEXT ACTION:** none of the 3 deferred candidates are scheduled — each would need its own dedicated task with the same rigor already applied, not a quick-pass attempt. `KNOWN_ISSUES.md` #16 is now at 10/~16, all 10 fully shipped to both BACKUP and production, with 3 candidates evidence-deferred and remaining pages (`Map`/`Dashboard`/`Account`/`InHome`/`FdePortal`/`AdbustingPortal`/`GraffitiPortal`) intentionally untouched pending their own separate work.

## NOTED, NOT ACTED ON — `feat/weather-context-v1` branch state (read-only inspection only), 2026-09-22

- **STATUS:** INSPECTED, NOT MODIFIED (per explicit instruction — no touch, no reset, no rebase, no merge).
- **FINDING:** despite its name, this branch's own commit history (`git log feat/weather-context-v1`) contains **zero** weather-related commits — its most recent commits are about production-observability telemetry and an unrelated map dateline-query fix. Its current **uncommitted, dirty working tree** (the actual live state of the checkout at the start of this session) is *also* not weather-related — the modified/untracked files are about TrueCost/UPC-scanning (`TrueCostResult.jsx`, `UpcScanner.jsx`, `AdScanLab.jsx`, `productLookup/`, `productIdentifier.js`, `productResolver.js`, etc.), not weather context.
- **CONCLUSION:** "weather context" work has not actually been started in this branch, despite the branch name — either it's a planned-but-not-yet-begun next step after the current in-progress TrueCost work, or the branch was named ahead of a pivot that hasn't happened yet. This is a naming/expectation mismatch worth surfacing to whoever is tracking that branch, not a silent assumption to carry forward.
- **NEXT ACTION:** none from this session — flagging only, per the explicit read-only instruction.
